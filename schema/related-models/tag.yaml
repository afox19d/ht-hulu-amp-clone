name: Tag
source: snowflake-test
description: tag user by tag_name/tag_id for segment analysis
type: table
rawSql: |-
  SELECT user_id::string as user_id_str, *
  FROM INPUT.AUDIENCE_TAG_VW
tableName: INPUT.AUDIENCE_TAG_VW
